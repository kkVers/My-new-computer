#pragma warning(disable:4996)
#pragma warning(disable:6031)
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

double Mean(double a[m][n], int m, int n) {
	int v;

	return v;
}

void main() {
	//编写一个函数，函数头格式为“double Mean(double a[M][N],int m,int n)”，要求返回二维数组a[m][n]中所有元素的平均值，假定在计算过程中采用变量v存放平均值。
	

	//char a[] = "abcdbfbgacd";
	//int i1 = 0, i2 = 0, i = 0;
	//while (a[i]) {
	//if (a[i] == 'a') i1++;
	//if (a[i] == 'b') i2++;
	//i++;
	//}printf("%d %d %d\n", i1, i2, i);

	////
	//int a[3][4]={{1,2,7,8},{5,6,10,6},{9,12,3,4}};

	//int m = a[0][0];
	//int i, j;
	//for (i = 0; i < 3; i++)
	//for (j = 0; j < 4; j++)
	//if (a[i][j] > m) m = a[i][j];
	//printf("%d\n", m);

	////求不等式2^2+4^2...+n^2<1000的n最值，用i和s取偶数值和累加值的变量限定用do的循环
	//int i = 2, s = 0;
	//do {
	//	if (i % 2 == 0)s += i * i;
	//	i+=2;
	//	if (s > 1000)	break
	//} while (s < 1000);
	//printf("%d", i);

	////计算1^2+2^2+...+n^2的值
	//int i=1,s=0,n;
	//scanf("%d", &n);
	//while (n) {
	//	s = s + i * i;
	//	i++;
	//	n--;
	//}
	//printf("%d", s);

	//int n = 6, y = 1;
	//while (n) {
	//	y *= n--;
	//	printf("y=%d\n", y);
	//}	

	/*int i, s1 = 0, s2 = 0;
	for ( i = 0; i < 10; i++)
	{
		if (i % 2)s1 += i; else s2 += i;
		printf("%d %d\n", s1, s2);
	}*/

	/*int i, s = 0;
	for (i = 1; i <= 5; i++) {
		s += i * i;
	}
	printf("s=%d\n", s);*/
}